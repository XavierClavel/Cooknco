package com.xavierclavel.cooknco

import kotlin.test.Test
import kotlin.test.assertEquals

/**
 * Example test in `commonTest`, which runs on every target.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
