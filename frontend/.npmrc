# npm's resolver crashes ("Cannot read properties of null (reading 'edgesOut')")
# building the strict peer tree for nuxt 4 + its module ecosystem. Legacy
# resolution installs the same versions without the crash, and keeps `npm ci`
# in CI consistent with local installs.
legacy-peer-deps=true
